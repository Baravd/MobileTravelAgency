<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bvd.android.agentie">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".client.ClientActivity" />
        <activity android:name=".employee.EmployeeActivity" />
        <activity android:name=".employee.TripDetailsActivity" />
        <activity android:name=".employee.AddTripActivity" />
        <activity android:name=".client.CustomerMyItemsActivity" />
        <activity android:name=".client.CustomerAllItemsActivity" />
        <activity android:name=".client.CustomerReserveActivity" />
        <activity android:name=".client.CustomerCancelReservationActivity"></activity>
    </application>

</manifest>